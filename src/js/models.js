import {
  CallTree,
  Components,
  EventInfo,
  EventSource,
  getLabel,
} from '@appland/models';

const Models = {
  CallTree,
  Components,
  EventInfo,
  EventSource,
  getLabel,
};

export default Models;
