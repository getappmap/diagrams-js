import {
  ClassMap,
  CallTree,
  Components,
  EventInfo,
  EventSource,
  getLabel,
} from '@appland/models';

const Models = {
  ClassMap,
  CallTree,
  Components,
  EventInfo,
  EventSource,
  getLabel,
};

export default Models;
