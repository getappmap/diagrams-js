import {
  CallTree,
  Components,
  EventInfo,
  EventSource,
  getLabel,
} from '@applandinc/appmap-models';

const Models = {
  CallTree,
  Components,
  EventInfo,
  EventSource,
  getLabel,
};

export default Models;
